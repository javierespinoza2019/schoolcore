/**
 * Feature flags del cliente (stub MVP).
 * Más adelante se alimentarán desde API (Branch > Tenant > Global).
 */
export const FEATURES = {
  /** Asistente IA — fuera del MVP; fase final del roadmap. */
  aiAssistant: false,
} as const;
